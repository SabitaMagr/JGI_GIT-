<?php
$designationTitle = $form->get("designationTitle");
$designationTitle->setAttribute('class','form-name form-control');
$designationTitle->setAttribute("placeholder","Designation Title");


$designationDetail = $form->get("designationDetail");
$designationDetail->setAttribute("class","form-name form-control");
$designationDetail->setAttribute("placeholder","Designation Detail");

$submit = $form->get("submit");
$submit->setAttribute("class","btn");
$submit->setAttribute("value","Add Designation");

$form->setAttribute('action', $this->url('designation', ['action' => 'add']));

if ($this->messages) {
    print '<ul class="help-block bg-danger" style="background:#f3e250;">';
    foreach ($this->messages as $message) {
        print '<li>' . $message . '</li>';
    }
    print '</ul>';
}

$form->prepare();
print $this->form()->openTag($form);
?>
<div class="row">
    <div class="col-sm-12">
        <div class="panel panel-default">
            <div class="panel-heading panel-title-head">
                <!--<div class="panel-title"> -->
                    <h4>Designation Setup</h4>     
            </div>
            <div class="panel-body padding-5" style="margin-top:7px;">  
                <div class="form-group form-group-default emp-form-group">
                    <?= $this->formLabel($designationTitle) ?>
                    <?= $this->formElement($designationTitle); ?>
                    <?= $this->formElementErrors()->render($designationTitle, ['class' => 'help-block']) ?>
                </div>
                <div class="form-group form-group-default emp-form-group">
                    <?= $this->formLabel($designationDetail) ?>
                    <?= $this->formElement($designationDetail); ?>
                    <?= $this->formElementErrors()->render($designationDetail, ['class' => 'help-block']) ?>
                </div>
                <div class="panel-footer panel-footer-default">
                    <?php  print $this->formSubmit($submit); ?>     
                </div>
            </div>
        </div>
    </div>
</div>

<?php

print $this->form()->closeTag();
?>
