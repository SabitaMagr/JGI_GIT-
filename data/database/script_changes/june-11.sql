CREATE TABLE HRIS_APPRAISAL_COMPETENCY
  (
    APPRAISAL_ID  NUMBER(7,0) NOT NULL,
    EMPLOYEE_ID   NUMBER(7,0) NOT NULL,
    SNO           NUMBER(7,0) NOT NULL,
    TITLE         VARCHAR2(200 BYTE) NOT NULL,
    RATING        VARCHAR2(400 BYTE) NOT NULL,
    COMMENTS      VARCHAR2(4000 BYTE) DEFAULT NULL,
    COMPANY_ID    NUMBER(7,0),
    BRANCH_ID     NUMBER(7,0) DEFAULT NULL,
    CREATED_BY    NUMBER(7,0),
    CREATED_DATE  DATE,
    MODIFIED_BY   NUMBER(7,0),
    MODIFIED_DATE DATE,
    CHECKED       VARCHAR2(1 BYTE) DEFAULT 'N',
    APPROVED_BY   NUMBER(7,0),
    APPROVED_DATE DATE,
    APPROVED      VARCHAR2(1 BYTE) DEFAULT 'N',
    STATUS        CHAR(1 BYTE) CHECK (STATUS IN('E','D'))
  );
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT PK_APPRAISAL_COM PRIMARY KEY (SNO);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_APP_APP_ID FOREIGN KEY (APPRAISAL_ID) REFERENCES HRIS_APPRAISAL_SETUP(APPRAISAL_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_COM_COMPANY_ID FOREIGN KEY(COMPANY_ID) REFERENCES HRIS_COMPANY(COMPANY_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_BRN_BRANCH_ID FOREIGN KEY(BRANCH_ID) REFERENCES HRIS_BRANCHES(BRANCH_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_EMP_EMPLOYEE_ID FOREIGN KEY(EMPLOYEE_ID) REFERENCES HRIS_EMPLOYEES(EMPLOYEE_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_EMP_EMPLOYEE_ID1 FOREIGN KEY(CREATED_BY) REFERENCES HRIS_EMPLOYEES(EMPLOYEE_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_EMP_EMPLOYEE_ID2 FOREIGN KEY(MODIFIED_BY) REFERENCES HRIS_EMPLOYEES(EMPLOYEE_ID);
ALTER TABLE HRIS_APPRAISAL_COMPETENCY ADD CONSTRAINT FK_APP_COM_EMP_EMPLOYEE_ID3 FOREIGN KEY(APPROVED_BY) REFERENCES HRIS_EMPLOYEES(EMPLOYEE_ID);

ALTER TABLE HRIS_SHIFTS MODIFY END_DATE DATE NOT NULL;
  

ALTER TABLE HRIS_SHIFTS DROP COLUMN SHIFT_LNAME;
  
ALTER TABLE HRIS_SHIFTS ADD SHIFT_LNAME VARCHAR2(150) NULL;
