INSERT
INTO HRIS_MENUS
  (
    MENU_CODE,
    MENU_ID,
    MENU_NAME,
    PARENT_MENU,
    MENU_DESCRIPTION,
    ROUTE,
    STATUS,
    CREATED_DT,
    MODIFIED_DT,
    ICON_CLASS,
    ACTION,
    MENU_INDEX,
    CREATED_BY,
    MODIFIED_BY,
    IS_VISIBLE
  )
  VALUES
  (
    NULL,
    328,
    'Attendnace Report',
    5,
    NULL,
    'managerReport',
    'E',
    TRUNC(SYSDATE),
    NULL,
    'fa fa-pencil',
    'index',
    19,
    NULL,
    NULL,
    'Y'
  );
ALTER TABLE HRIS_EMPLOYEES
ADD IS_DCEO CHAR (1 BYTE) CHECK (IS_DCEO IN ('Y','N'));

ALTER TABLE HRIS_EMPLOYEE_TRAVEL_REQUEST
ADD APPROVER_ROLE VARCHAR2(50 BYTE);

ALTER TABLE HRIS_EMP_TRAVEL_EXPENSE_DTL
ADD
FARE_FLAG CHAR(1 BYTE) CHECK (FARE_FLAG IN ('Y','N'));

ALTER TABLE HRIS_EMP_TRAVEL_EXPENSE_DTL
ADD
ALLOWANCE_FLAG CHAR(1 BYTE) CHECK (ALLOWANCE_FLAG IN ('Y','N'));

ALTER TABLE HRIS_EMP_TRAVEL_EXPENSE_DTL
ADD
LOCAL_CONVEYENCE_FLAG CHAR(1 BYTE) CHECK (LOCAL_CONVEYENCE_FLAG IN ('Y','N'));

ALTER TABLE HRIS_EMP_TRAVEL_EXPENSE_DTL
ADD
MISC_EXPENSES_FLAG CHAR(1 BYTE) CHECK (MISC_EXPENSES_FLAG IN ('Y','N'));

